# Local Model Assets

Model assets are organized by capability, not by implementation language.

Current Beta layout:

- `tts/`
- `stt/`
- `llm/`

Example:

- Kokoro belongs under `models/tts/kokoro/`
