[package]
name = "marginalia-core"
version = "0.1.0"
edition = "2021"
description = "Shared Marginalia Beta engine core"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
sha2 = "0.10"
