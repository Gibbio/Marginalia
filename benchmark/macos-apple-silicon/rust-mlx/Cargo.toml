[package]
name = "kokoro-mlx-probe"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
mlx-rs = { version = "0.25", features = ["safetensors"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
