This repository is part of Marginalia.

Follow `AGENTS.md` as the source of truth.

When generating code:

- preserve documented TUI / CLI / mac-gui behavior
- do not invent new features
- do not change `marginalia.toml` schema
- update docs/tests when behavior changes
- avoid scope creep
